@namespace("sample.user.v1")
protocol SampleEvents {
  import idl "../common/SharedMeta.v1.avdl";
  record UserEvent {
    sample.common.SharedMeta meta;
    array<sample.common.SharedMeta> searchResults;
    int userId;
  }
}